import { PepsiStripe } from '@/components/brand/PepsiStripe'

/** @deprecated Use `PepsiStripe` — kept for existing imports */
export function AdminPepsiStripe(props: { className?: string }) {
  return <PepsiStripe {...props} />
}
