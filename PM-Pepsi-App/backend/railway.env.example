# คัดลอกค่าจาก PM-Pepsi-App/backend/.env ไป Railway → Variables
# อย่า commit ไฟล์นี้ถ้าใส่รหัสผ่านจริง — ใช้เป็น checklist เท่านั้น

DATABASE_URL=
SESSION_SECRET=
NODE_ENV=production
CORS_ORIGIN=
APP_PUBLIC_URL=
BACKUP_SCHEDULER=0
INTEGRATION_WATCH_SCHEDULER=0
TELEGRAM_NOTIFY_ENABLED=false

# Railway ตั้ง PORT ให้อัตโนมัติ — ไม่ต้องใส่ PORT
