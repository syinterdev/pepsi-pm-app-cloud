-- 059 — Menu Builder: ซ่อนเฉพาะแถบ Admin ด้านใน (React hideInSideNav)
-- ไม่ลบ tbmenu — เข้าจาก sidebar หลัก / Admin Console / ⌘K ตาม 056
-- ถ้าเคยรัน migration เก่าที่ DELETE แล้ว ให้รัน 060_restore_admin_menu_tbmenu.sql

SELECT 1;
