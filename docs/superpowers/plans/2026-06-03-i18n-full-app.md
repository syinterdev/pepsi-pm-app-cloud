# i18n — Full app (EN default + TH)

**Scope:** Frontend UI only (`PM-Pepsi-App/frontend`).

## Infrastructure

- [x] `i18next` + 17 namespaces registered in `src/i18n/index.ts`
- [x] `pm-app.locale` + LanguageSwitcher + Settings profile
- [x] `useI18nFormat()` for calendars
- [x] `localize-nav.ts` + English fallback `nav-config.ts`

## Namespaces

`common`, `nav`, `home`, `planning`, `scheduling`, `confirmation`, `workOrders`, `errors`, `personnel`, `manhours`, `reports`, `admin`, `integration`, `board`, `pmVibration`, `masterData`, `userLog`

## Sprint status

| Sprint | Status |
|--------|--------|
| S0 errors, toast, auth | done |
| S1 scheduling + WO dialog | done |
| S2 confirmation, workOrders | done |
| S3 personnel | done |
| S4 manhours, reports, board, pmVibration | done |
| S5 integration, iw37n, masterData | done |
| S6 admin | done |
| S7 QA + leftovers | mostly done |

## Remaining (non-blocking)

- `features/dev/UiPlaygroundPage.tsx` — dev-only demo strings
- Code comments in Thai (not user-visible)
- `lib/localize-nav.ts` — Thai heading keys for DB menu mapping (intentional)
- Some master-data entity field labels (per-entity dialogs)
- API / SAP codes / WO numbers — never translated

## Verify

1. Toggle EN/TH on top bar — spot-check `/`, `/calendar`, `/work-orders`, `/personnel`, `/admin`
2. EN: no stray Thai on labels/buttons/empty states
3. TH: UAT column TH on `UI-POLISH-PHASES.md`
4. `cd PM-Pepsi-App/frontend && npx tsc --noEmit && npm test -- --run`
